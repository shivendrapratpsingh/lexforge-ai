// Simple pass-through layout for auth pages (/login, /register)
// Uses root layout (Providers + globals.css) — no extra wrapper needed
export default function AuthLayout({ children }) {
  return children
}
